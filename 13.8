#include <iostream>
#include <fstream>
#include <string.h>

using namespace std;

int main()
{
	fstream f;
	f.open("8.txt", ios::out | ios::trunc);

	string B("hl");
	string A = "hdshluijghpa";
	char *p = new char [B.length()];

	for (int i = 0; i < B.length(); i++) {

		p[i] = B[i];

	}
	
	f << B << endl;
	f << A << endl;

	for (int i = 0; i < A.length(); i++)
	{
		for (int j = 0; j < B.length(); j++)
		{
			if (A[i] == B[j])
			{
				A.erase(i, 1);
			}
		}
	}

	for (int i = 0; i < A.length(); i++)
	{
		cout << A[i];
		f << A[i];
	}
}

//Составить алгоритм, исключающий из строки А символы строки Б.
